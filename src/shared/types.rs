pub enum Mode{
    Fetch,
    System,
    User,
    Session,
    Env,
    Networks,
}

pub enum Action{
    Quit,
    ToggleHelp,
    EnterFetch,
    EnterSystem,
    EnterUser,
    EnterSession,
    EnterEnv,
    EnterNetworks,
}
